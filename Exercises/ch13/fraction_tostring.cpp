#include <iostream>

struct Fraction {
    int numerator, denominator;


    Fraction();
    Fraction(int n, int d);

    string to_string();
};

string Fraction :: to_string() {
    if  (denominator == 1) {
        return 
    }
    return 
}